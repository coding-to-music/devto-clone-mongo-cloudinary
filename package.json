{
  "name": "devto-clone-mongo-cloudinary",
  "version": "1.0.0",
  "scripts": {
    "stack": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "deploy": "git add . && git commit -m Build && git push"
  },
  "author": "marwanmodev",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.2.1"
  },
  "dependencies": {
    "pm2": "^5.2.0"
  }
}
